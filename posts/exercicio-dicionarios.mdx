---
path: /exercicios-dicionarios-1
title: "Exercício #1"
date: 2020-05-19
episode: Dicionários
cover: https://images.unsplash.com/photo-1488590528505-98d2b5aba04b?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1907&q=80
author_name: "Cassios Marques"
author_email: "cassios.kmm@gmail.com"
highlight: true
tags: ["exercicio", "programacao"]
---
import Logo from "../src/components/Logo"

## Introdução
Dicionário é uma coleção de dados **desordenados** estruturados na forma de pares chave/valor. As chaves são strings e os valores podem ser de qualquer tipo: inteiro, decimal, booleano, lista e até mesmo outro dicionário.
Em Python, dicionários são definidos com *{}*.

Veja abaixo o exemplo de um dicionário que representa as características de uma cadeira e as operações que podemos fazer.

```python
# Exemplo de dicionário que mapeia as características de uma cadeira
chair = {
    'weight': 18.5,
    'dimensions': '129x69x64cm',
    'available_colors': ['black', 'blue'],
    'price': 764.10,
    'customer_stars': 3.7
}

### Operações básicas ###
# imprimir na tela o dicionário completo
print(chair)

# acessar um atributo do dicionário
weight = chair['weight']
print(weight)

# mudar o valor de um atributo
chair['price'] = 811.99
print(chair)

# verificar se uma chave existe no dicionário
print('customer_stars' in chair)
print('brand' in chair)

# verificar o número de itens do dicionário
size = len(chair)
print(size)

# remover um item do dicionário
del(chair['dimensions'])
print(chair)

# adicionar um item ao dicionário
chair['brand'] = 'SuperChairs'
print(chair)

# iterar sob as chaves do dicionário
print('Printing dictionary keys:')
for key in chair.keys():
    print(key)

# iterar sob os valores do dicionário
print('Printing dictionary values:')
for value in chair.values():
    print(value)

# iterar sob as chaves e valores do dicionário
print('Printing dictionary keys and values:')
for key, value in chair.items():
    print(key)
    print(value)
    print('') # imprimir linha em branco
```


## Exercícios - Agenda PodCodar
A PodCodar está crescendo e precisa da sua ajuda para fazer um programa que gerencie as informações dos alunos.
Gostaríamos de poder adicionar novos alunos, consultar as informações de um aluno, consultar presença e entrega das listas de exercícios, entre outras funcionalidades.

O tipo dicionário que acabamos de aprender é uma boa estrutura para guardarmos os dados dos alunos. A chave deverá ser o *alias* do aluno e o valor um outro dicionário com as informações do aluno. Veja o exemplo a seguir:
```python
# A agenda da PodCodar será definida como um dicionário tal que:
# - a chave é um alias que identifica a pessoa
# - o valor é um dicionário com todas as informações da pessoa
podcodar_notebook = {
    'b1': {
        'full_name': 'Banana B1',
        'city': 'Belo Horizonte',
        'dob': '01/01/1990', # dob means date of birth
        'email': 'b1@hotmail.com',
        'buddy': 'Luiz'
    },
    'b2': {
        'full_name': 'Banana B2',
        'city': 'São Paulo',
        'dob': '01/01/1990',
        'email': 'b2@hotmail.com',
        'buddy': 'Marquin'
    },
    'b3': {
        'full_name': 'Banana B3',
        'dob': '01/01/1992',
        'email': 'b2@hotmail.com',
    },
}
```
Com esta estrutura de dados é possível guardar estaticamente as informações dos nossos alunos. Contudo, ainda precisamos implementar algumas funcionalidades para deixar a agenda mais dinâmica e facilitar as consultas. Vamos lá?

a) implemente uma função que imprime na tela todas as informações de uma pessoa.
```python
def print_person_info(notebook, alias):
    # TODO
```

b) implemente uma função que imprime na tela a cidade de uma pessoa.
```python
def print_person_city(notebook, alias):
    # TODO
```

c) implemente uma função que imprime na tela uma informação específica de uma pessoa. Se a informação requisitada não existir, imprimir na tela um aviso que a informação não existe.
```python
def print_person_specific_info(notebook, alias, info_key):
    # TODO
```

d) implemente uma função que imprime na tela o número de pessoas que utilizam Gmail.
```python
def count_gmail_users(notebook, alias):
    # TODO
```

e) implemente uma função que imprime na tela a idade de uma pessoa. Para isso, utilize a função abaixo que retorna o ano dado uma data de aniversário no formato string.
```python
# recebe uma data no formato string e retorna o ano nascimento no formato inteiro. Ex:
# get_year_of_birth('15/06/1990') == 1990
def get_year_of_birth(dob):
    return int(dob[-4:])

def print_age(notebook, alias):
    # TODO
```

f) implemente uma função que adiciona as informações de uma nova pessoa à agenda. Se a pessoa já estiver na agenda, um erro deverá ser impresso na tela e o registro existente NÃO deverá ser alterado.
```python
def add_person_info(notebook, alias, info):
    # TODO
```

g) modifique a função anterior para verificar se as seguintes informações foram providas pelo usuário: *full_name* e *email*.
Se faltarem estas informações um erro deverá ser impresso na tela e o registro NÃO deverá ser adicionado à agenda.

h) implemente uma função que apaga uma pessoa da agenda. Se a pessoa não existir, um aviso deverá ser impresso na tela.
```python
def delete_person(notebook, alias):
    # TODO
```

## Desafios
A PodCodar gostou muito dos seus serviços e decidiu te contratar pra desenvolver mais uma funcionalidade para a nossa agenda.
Gostaríamos de acompanhar as entregas de para-casa dos alunos. Para tal, adicione uma nova propriedade às informações do usuário na qual serão listados os exercícios entregues. Ex:
```python
podcodar_notebook = {
    'b1': {
        'full_name': 'Banana B1',
        'city': 'Belo Horizonte',
        'dob': '01/01/1990', # dob means date of birth
        'email': 'b1@hotmail.com',
        'buddy': 'Luiz',
        'deliveredHomeworks': ['homeworkWeek1', 'homeworkWeek4']
    },
}
```
a) implemente uma função que adiciona um para-casa à lista de para-casa de uma pessoa.
```python
def set_homework_completion(notebook, alias, homework):
    # TODO
```

b) implemente uma função que imprime na tela a quantidade de para-casa que um aluno entregou.
```python
def count_homeworks(notebook, alias):
    # TODO
```

c) implemente uma função que imprime na tela a quantidade de para-casa que cada aluno entregou.
```python
def count_homeworks_for_all(notebook):
    # TODO
```

d) implemente uma função que imprime na tela quantas pessoas entregaram um para-casa.
```python
def count_homework_completion(notebook, homework):
    # TODO
```

## Dúvidas?
Comenta lá no nosso grupo do Discord ou pergunte ao seu padrinho.


## Entrega

Nomeie o arquivo de código com o seu nome (ex: antonio_barbosa.py) e faça o upload na seguinte pasta do GDrive:
https://drive.google.com/drive/folders/1wwQhNdeU_lyFmbb15JoHIo_WEg2FHx25?usp=sharing

<div style={{alignItems: "center", display: "flex", justifyContent: "center"}}>
    <Logo/>
</div>